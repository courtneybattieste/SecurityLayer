# New File Operation

try:
    # Open File Function Call
    myfile=ABopenfile("TestFile.txt",True)  #Create an AB file

    # Close the file
    myfile.close()

except:
    # Close the file
    myfile.close()

    #Error Handle or Failure Condition
    log("Uppercase Letters in File Name not handled correctly.")
